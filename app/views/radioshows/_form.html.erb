<%= form_with(model: radioshow, local: true) do |form| %>
  <% if radioshow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(radioshow.errors.count, "error") %> prohibited this radioshow from being saved:</h2>

      <ul>
      <% radioshow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :radioshow_title %>
  </div>

  <div class="field">
    <%= form.label :episode %>
    <%= form.text_field :episode, id: :radioshow_episode %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :radioshow_description %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :radioshow_image_url %>
  </div>

  <div class="field">
    <%= form.label :listen_url %>
    <%= form.text_field :listen_url, id: :radioshow_listen_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
